services:
  - type: web
    name: tcg-order-fulfillment-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
    healthCheckPath: /api/health
